"use client";

const PageTransition = ({ children }) => {
  // Simplificado completamente - no hay transiciones necesarias para una sola página
  // Esto elimina cualquier delay o flash visual
  return <>{children}</>;
};

export default PageTransition;
